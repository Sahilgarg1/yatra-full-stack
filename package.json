{
  "name": "yatra-full-stack",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently --names \"api,web\" --prefix-colors \"green,cyan\" \"npm run dev --prefix trip-api\" \"npm run dev --prefix web\"",
    "start": "concurrently --names \"api,web\" --prefix-colors \"green,cyan\" \"npm run dev --prefix trip-api\" \"npm run dev --prefix web\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
